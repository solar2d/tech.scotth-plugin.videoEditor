Sources for the plugin `plugin.videoEditor`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.videoEditor" = {
            publisherId = "tech.scotth",
        },
    },
}
```
